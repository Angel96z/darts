import 'package:flutter/material.dart';

class GiocaScreen extends StatelessWidget {
  const GiocaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Sezione Gioca"),
    );
  }
}