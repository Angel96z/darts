enum RoomStatus { waiting, inGame, terminated, closed }

enum MatchStatus { notStarted, active, finished }

enum RoomRole { host, player, spectator }

enum ParticipantType { authUser, externalGuest, localGuest }
