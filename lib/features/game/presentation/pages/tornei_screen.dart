/// File: tornei_screen.dart. Contiene logica di presentazione (UI, widget o controller) per questa parte dell'app.

import 'package:flutter/material.dart';

class TorneiScreen extends StatelessWidget {
  /// Funzione: descrive in modo semplice questo blocco di logica.
  const TorneiScreen({super.key});

  @override
  /// Funzione: descrive in modo semplice questo blocco di logica.
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Sezione Tornei"),
    );
  }
}
