import 'package:flutter/material.dart';

class CampionatiScreen extends StatelessWidget {
  const CampionatiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Sezione Campionati"),
    );
  }
}