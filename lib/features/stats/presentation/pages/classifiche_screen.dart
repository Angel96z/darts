import 'package:flutter/material.dart';

class ClassificheScreen extends StatelessWidget {
  const ClassificheScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Sezione Classifiche"),
    );
  }
}