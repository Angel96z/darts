/// File: classifiche_screen.dart. Contiene logica di presentazione (UI, widget o controller) per questa parte dell'app.

import 'package:flutter/material.dart';

class ClassificheScreen extends StatelessWidget {
  /// Funzione: descrive in modo semplice questo blocco di logica.
  const ClassificheScreen({super.key});

  @override
  /// Funzione: descrive in modo semplice questo blocco di logica.
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Sezione Classifiche"),
    );
  }
}
